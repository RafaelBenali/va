# =============================================================================
# TNSE - Telegram News Search Engine
# Render.com Environment Variables
# =============================================================================
#
# Work Stream: WS-4.1 - Render.com Configuration
#
# This file documents the environment variables needed for Render.com deployment.
# Most variables are automatically set by Render's Blueprint (render.yaml).
#
# Variables marked [AUTO] are automatically provided by Render.com
# Variables marked [MANUAL] must be set manually in the Render Dashboard
#
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings [AUTO]
# These are configured in render.yaml
# -----------------------------------------------------------------------------
APP_ENV=production
DEBUG=false
LOG_LEVEL=INFO
WORKERS=2

# Port is set by Render.com automatically
# PORT=10000  # [AUTO] Render assigns this dynamically

# -----------------------------------------------------------------------------
# Secret Key [AUTO]
# Generated automatically by Render using generateValue: true
# -----------------------------------------------------------------------------
# SECRET_KEY=<auto-generated>  # [AUTO]

# -----------------------------------------------------------------------------
# Database Configuration [AUTO]
# Render.com provides these from the managed PostgreSQL database
# -----------------------------------------------------------------------------
# DATABASE_URL=<connection-string>  # [AUTO] Full connection string
# POSTGRES_HOST=<hostname>  # [AUTO]
# POSTGRES_PORT=5432  # [AUTO]
# POSTGRES_DB=tnse  # [AUTO]
# POSTGRES_USER=tnse_user  # [AUTO]
# POSTGRES_PASSWORD=<password>  # [AUTO]

# -----------------------------------------------------------------------------
# Redis Configuration [AUTO]
# Render.com provides these from the managed Redis instance
# -----------------------------------------------------------------------------
# REDIS_URL=<connection-string>  # [AUTO] Full connection string
# REDIS_HOST=<hostname>  # [AUTO]
# REDIS_PORT=6379  # [AUTO]

# -----------------------------------------------------------------------------
# Celery Configuration [AUTO]
# Uses Redis connection strings from Render
# -----------------------------------------------------------------------------
# CELERY_BROKER_URL=<redis-url>  # [AUTO]
# CELERY_RESULT_BACKEND=<redis-url>  # [AUTO]

# -----------------------------------------------------------------------------
# Telegram Bot Configuration [MANUAL]
# YOU MUST SET THESE IN THE RENDER DASHBOARD
# -----------------------------------------------------------------------------

# Get your bot token from @BotFather on Telegram
# 1. Message @BotFather on Telegram
# 2. Send /newbot and follow prompts
# 3. Copy the token provided
TELEGRAM_BOT_TOKEN=your_bot_token_here  # [MANUAL] REQUIRED

# Get API credentials from https://my.telegram.org/apps
# 1. Go to https://my.telegram.org
# 2. Log in with your phone number
# 3. Go to "API development tools"
# 4. Create an application
TELEGRAM_API_ID=your_api_id  # [MANUAL] REQUIRED
TELEGRAM_API_HASH=your_api_hash  # [MANUAL] REQUIRED

# Telegram phone for session (optional, for MTProto)
TELEGRAM_PHONE=+1234567890  # [MANUAL] Optional

# -----------------------------------------------------------------------------
# Access Control [MANUAL]
# Restrict bot access to specific Telegram user IDs
# -----------------------------------------------------------------------------

# Comma-separated list of Telegram user IDs allowed to use the bot
# Leave empty for unrestricted access
# Find your user ID by messaging @userinfobot on Telegram
ALLOWED_TELEGRAM_USERS=123456789,987654321  # [MANUAL] Optional

# -----------------------------------------------------------------------------
# Content Processing Settings [AUTO/OPTIONAL]
# These have sensible defaults but can be customized
# -----------------------------------------------------------------------------

# Time window for content collection (in hours)
CONTENT_WINDOW_HOURS=24  # Default: 24

# Interval for content collection jobs (in minutes)
COLLECTION_INTERVAL_MINUTES=15  # Default: 15

# Maximum channels to process per batch
MAX_CHANNELS_PER_BATCH=50  # Default: 50

# -----------------------------------------------------------------------------
# Search Settings [AUTO/OPTIONAL]
# -----------------------------------------------------------------------------

# Maximum results per search query
MAX_SEARCH_RESULTS=100  # Default: 100

# Cache TTL for search results (in seconds)
SEARCH_CACHE_TTL=300  # Default: 300 (5 minutes)

# -----------------------------------------------------------------------------
# LLM Settings [MANUAL/OPTIONAL]
# Only needed if using LLM-powered semantic analysis
# -----------------------------------------------------------------------------

# Enable LLM mode
LLM_ENABLED=false  # Default: false

# LLM Provider (openai or anthropic)
LLM_PROVIDER=openai  # Default: openai

# OpenAI API Key (if using OpenAI)
# OPENAI_API_KEY=sk-...  # [MANUAL] Optional

# Anthropic API Key (if using Anthropic)
# ANTHROPIC_API_KEY=sk-ant-...  # [MANUAL] Optional
